<ul class="nav nav-tabs" id="top-nav">
  <li class="nav-item" id='home'>
            {% if home %}
            <a class="nav-link active" href="{{url_for('index')}}">Home</a>
            {% else %}
            <a class="nav-link" href="{{url_for('index')}}">Home</a>
            {% endif %}
        </li>
        <li class="nav-item">
            {% if events %}
            <a class="nav-link active" href="{{url_for('main.events')}}">Events</a>
            {% else %}
            <a class="nav-link" href="{{url_for('main.events')}}">Events</a>
            {% endif %}
        </li>
        <li class="nav-item">
            {% if music %}
            <a class="nav-link active" href="{{url_for('main.music')}}">Music</a>
            {% else %}
            <a class="nav-link" href="{{url_for('main.music')}}">Music</a>
            {% endif %}
        </li>
        <li class="nav-item">
            {% if devotions %}
            <a class="nav-link active" href="{{url_for('main.devotions')}}">Devotions</a>
            {% else %}
            <a class="nav-link" href="{{url_for('main.devotions')}}">Devotions</a>
            {% endif %}
        </li>
        <li class="nav-item">
            {% if message %}
            <a class="nav-link active" href="{{url_for('main.messages')}}">Messages</a>
            {% else %}
            <a class="nav-link" href="{{url_for('main.messages')}}">Messages</a>
            {% endif %}
        </li>
        <li class="nav-item">
            {% if profile %}
            <a class="nav-link active" href="{{url_for('main.profile')}}">Profile</a>
            {% else %}
            <a class="nav-link" href="{{url_for('main.profile')}}">Profile</a>
            {% endif %}
        </li>
        {% if current_user.is_anonymous %}
        <li class="nav-item">
            {% if login %}
            <a class="nav-link active" href="{{url_for('auth.login')}}">Login</a>
            {% else %}
            <a class="nav-link" href="{{url_for('auth.login')}}">Login</a>
            {% endif %}
        </li>
        <li class="nav-item">
            {% if register %}
            <a class="nav-link active" href="{{url_for('auth.register')}}">Register</a>
            {% else %}
            <a class="nav-link" href="{{url_for('auth.register')}}">Register</a>
            {% endif %}
        </li>
        {% else %}
        <li class="nav-item">
            {% if edit %}
            <a class="nav-link active" href="{{url_for('auth.edit_profile')}}">Edit Profile</a>
            {% else %}
            <a class="nav-link" href="{{url_for('auth.edit_profile')}}">Edit Profile</a>
            {% endif %}
        </li>
        <li class="nav-item">
            {% if logout %}
            <a class="nav-link active" href="{{url_for('auth.logout')}}">Logout</a>
            {% else %}
            <a class="nav-link" href="{{url_for('auth.logout')}}">Logout</a>
            {% endif %}
        </li>
        {% endif %}
</ul>

{# <a href="{{url_for('main.events')}}">Events</a>
<a href="{{url_for('main.events')}}">Events</a> #}


{# <li class="nav-item dropdown">
    <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-expanded="false">Dropdown</a>
    <ul class="dropdown-menu">
      <li><a class="dropdown-item" href="#">Action</a></li>
      <li><a class="dropdown-item" href="#">Another action</a></li>
      <li><a class="dropdown-item" href="#">Something else here</a></li>
      <li><hr class="dropdown-divider"></li>
      <li><a class="dropdown-item" href="#">Separated link</a></li>
    </ul>
  </li> #}