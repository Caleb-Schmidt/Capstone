    <ul class="nav flex-column nav-pills" id="right-nav">
        <li class="nav-item">
            <a class="nav-link" href="{{url_for('main.selection')}}">Create Post</a>
        </li>
        {% if current_user.is_admin %}
        <li class="nav-item">
            {% if users %}
            <a class="nav-link active" href="{{url_for('main.show_admins')}}">All Users</a>
            {% else %}
            <a class="nav-link" href="{{url_for('main.show_admins')}}">All Users</a>
            {% endif %}
        {% endif %}
        </li>
    </ul>